#include "generate/prelude.h"
#include "kota/async/async.h"

import agent;
import config;
import extract;
import generate;

#include "generate/support.h"

namespace fs = std::filesystem;

TEST_SUITE(agent_loop) {};  // TEST_SUITE(agent_loop)
